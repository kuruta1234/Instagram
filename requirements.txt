click>=8.1
Pillow>=10.0
PyYAML>=6.0
python-dotenv>=1.0
rich>=13.0
Flask>=3.0

# 任意: 背景除去を使う場合は以下も追加でインストール
# pip install rembg onnxruntime

# 任意: Claude Code CLIが使えない環境でAnthropic APIキーによる
# キャプション生成フォールバックを使う場合は以下も追加でインストール
# pip install anthropic
